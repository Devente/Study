# 构造函数
```
new 构造函数();
new做了4件事：
1、var obj = {}
2、obj.__proto__ = 构造函数.prototype  将这个空对象的__proto__成员指向了构造函数对象的prototype成员对象，这是最关键的一步
3、构造函数.call(obj)   将构造函数的作用域赋给新对象，因此构造函数中的this指向新对象obj，然后再调用构造函数，obj可以使用构造函数里的东西
4、return obj
```
